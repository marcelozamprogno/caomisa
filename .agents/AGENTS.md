# General Rules

- Always commit and push changes to GitHub (`git add .`, `git commit -m "..."`, `git push`) immediately after modifying files. Do NOT wait for the user to ask you to deploy or push. The user expects all local changes to go live via GitHub automatically.
